import { Navbar } from './Navbar.component';

export default {
  title: 'Atoms/Navbar',
  component: Navbar,
};

const Template = () => <Navbar name="Logo" />;

export const Default = Template.bind({});
