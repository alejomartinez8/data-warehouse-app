import { InputSearch } from './InputSearch.component';

export default {
  title: 'Molecules/InputSearch',
  component: InputSearch,
};

const Template = () => <InputSearch />;

export const Default = Template.bind({});
