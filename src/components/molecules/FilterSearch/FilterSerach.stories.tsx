import { FilterSearch } from './FilterSearch.component';

export default {
  title: 'Molecules/Filter Search',
  component: FilterSearch,
};

const Template = () => <FilterSearch />;

export const Default = Template.bind({});
